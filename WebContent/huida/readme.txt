1. 要使用conda虚拟环境，先在anaconda attempt新建一个环境，安装好所需配置，之后要打开pycharm设置python interpreter为conda 已创建好的环境.

2. （算法相关）无人机覆盖问题是指在给定的目标区域内，由无人机携带的传感器对目标区域实现扫描覆盖，覆盖路径规划则是通过规划无人机的最优扫描路径，满足覆盖任务要求等。

3. （python）创建一个类，在类里面创建的第一个函数__init__（）在类外面可以直接调用，即类名（）；
    set()是一个创建集合的内置函数，特点是无序、元素不重复

4. 让一个图层动态透明，可以用掩码，要先把地图转换成rgba，然后把需要透明坐标设置为0，最后imshow

5. 有关探索地图：第0层是已知区域简单图，第一层是未知区域全灰色，第二层是当前雷达范围，第三层是路径和机器人位置

问题：① 雷达扫描范围：可视化里面雷达范围增加0.5；√
     ② 机器人尺寸：markersize更改大小；√
     ③ 路径与障碍物太贴合：通过增加障碍物的 "安全距离"；√
     ④ 覆盖重合百分比。（下一优化内容）

下午三点开会，甲方可能会问的问题：
① 覆盖的指标？答：代码是全覆盖的路径探索规划，所以覆盖率是百分百；消耗的时间实际需要看小电脑的算力；因为这一阶段主要是把算法跑通，所以把重合率的计算以及降低放到下一次优化内容里面了。
② 选择目标点过程再详细介绍？答：目标点都是在边界点上进行选择的，遍历边界点计算与当前位置的曼哈顿距离，然后选择最近的点作为目标点

